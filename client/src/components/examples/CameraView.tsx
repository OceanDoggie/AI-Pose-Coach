import CameraView from '../CameraView';\nimport { POSE_TEMPLATES } from '@/data/poses';\nimport type { CameraSettings } from '@shared/schema';\n\nexport default function CameraViewExample() {\n  const mockSettings: CameraSettings = {\n    facingMode: \"user\",\n    autoShutter: true,\n    threshold: 80,\n    stableFrames: 10,\n    language: \"zh\",\n    coachingMode: \"simple\"\n  };\n\n  const handleScoreUpdate = (score: number, stable: boolean) => {\n    console.log('Pose score updated:', { score, stable });\n  };\n\n  const handlePhotoCapture = (imageData: string) => {\n    console.log('Photo captured:', imageData.slice(0, 50) + '...');\n  };\n\n  return (\n    <div className=\"h-screen flex flex-col bg-background-dark\">\n      <CameraView \n        selectedPose={POSE_TEMPLATES[0]}\n        settings={mockSettings}\n        onScoreUpdate={handleScoreUpdate}\n        onPhotoCapture={handlePhotoCapture}\n      />\n    </div>\n  );\n}\n"